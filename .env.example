# Copy to .env and fill in.
ANTHROPIC_API_KEY=sk-ant-...     # required (the writing)
APOLLO_API_KEY=...               # required (real prospects)
APOLLO_ENRICH=                   # leave BLANK. First names only, no credits. (1 would enrich; we don't.)
